<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.co.blogspot.programer27android.multiplescren13">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".list.CustomList" />
        <activity android:name=".spinner.CustomSpinner" />
        <activity android:name=".list.DetailListBuahActivity"/>
        <activity android:name=".video.Video" />
        <activity android:name=".viewpager.CustomViewPager"></activity>
    </application>

</manifest>